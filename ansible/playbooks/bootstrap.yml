- name: Bootstrap all servers
  hosts: all
  gather_facts: false
  become: true
  roles:
    - common
    - bootstrap_users
    - ssh_hardening

- name: Install Docker on app and monitoring servers
  hosts: app:monitoring
  gather_facts: true
  become: true
  roles:
    - docker
